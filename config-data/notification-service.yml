server:
  port: 0

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

spring:
  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: "256913a0654bdb"
    password: "de587cd87cc105"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  rabbitmq:
    listener:
      simple:
        retry:
          enabled: true
          initial-interval: 3000ms # Primera espera de 3 segundos
          max-attempts: 3          # Máximo 3 intentos totales
          max-interval: 10000ms   # No esperar más de 10 segundos
          multiplier: 2.0         # Backoff exponencial (3s, 6s, 10s)